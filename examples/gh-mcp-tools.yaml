- name: add_comment_to_pending_review
  description: "Add review comment to the requester's latest pending pull request review. A pending review needs to already exist."
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: pullNumber
      type: int
      description: "Pull request number"
      required: true
    - name: path
      type: str
      description: "The relative path to the file that necessitates a comment"
      required: true
    - name: body
      type: str
      description: "The text of the review comment"
      required: true
    - name: subjectType
      type: str
      description: "The level at which the comment is targeted (FILE or LINE)"
      required: true
    - name: line
      type: int
      description: "The line of the blob in the pull request diff that the comment applies to"
      required: false
    - name: side
      type: str
      description: "The side of the diff to comment on (LEFT or RIGHT)"
      required: false
  returns: "Confirmation of the review comment being added"

- name: add_issue_comment
  description: "Add a comment to a specific issue in a GitHub repository. Can also be used for pull request comments."
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: issue_number
      type: int
      description: "Issue number to comment on"
      required: true
    - name: body
      type: str
      description: "Comment content"
      required: true
  returns: "Created comment ID and URL"

- name: assign_copilot_to_issue
  description: "Assign GitHub Copilot to work on a specific issue and create a Pull Request with code changes."
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: issueNumber
      type: int
      description: "Issue number"
      required: true
  returns: "Confirmation that Copilot has been assigned to the issue"

- name: create_branch
  description: "Create a new branch in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: branch
      type: str
      description: "Name for new branch"
      required: true
    - name: from_branch
      type: str
      description: "Source branch (defaults to repo default)"
      required: false
  returns: "Created branch name and reference"

- name: create_or_update_file
  description: "Create or update a single file in a GitHub repository. If updating, provide the SHA of the file being replaced."
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: path
      type: str
      description: "Path where to create/update the file"
      required: true
    - name: content
      type: str
      description: "Content of the file"
      required: true
    - name: message
      type: str
      description: "Commit message"
      required: true
    - name: branch
      type: str
      description: "Branch to create/update the file in"
      required: true
    - name: sha
      type: str
      description: "The blob SHA of the file being replaced (required for updates)"
      required: false
  returns: "Commit SHA and file URL"

- name: create_pull_request
  description: "Create a new pull request in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: title
      type: str
      description: "Pull request title"
      required: true
    - name: head
      type: str
      description: "Branch containing changes"
      required: true
    - name: base
      type: str
      description: "Branch to merge into"
      required: true
    - name: body
      type: str
      description: "Pull request description"
      required: false
    - name: draft
      type: bool
      description: "Create as draft PR"
      required: false
    - name: maintainer_can_modify
      type: bool
      description: "Allow maintainer edits"
      required: false
  returns: "Created pull request number and URL"

- name: create_repository
  description: "Create a new GitHub repository in your account or specified organization"
  parameters:
    - name: name
      type: str
      description: "Repository name"
      required: true
    - name: description
      type: str
      description: "Repository description"
      required: false
    - name: private
      type: bool
      description: "Whether repo should be private"
      required: false
    - name: autoInit
      type: bool
      description: "Initialize with README"
      required: false
    - name: organization
      type: str
      description: "Organization to create the repository in"
      required: false
  returns: "Created repository URL and details"

- name: delete_file
  description: "Delete a file from a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: path
      type: str
      description: "Path to the file to delete"
      required: true
    - name: message
      type: str
      description: "Commit message"
      required: true
    - name: branch
      type: str
      description: "Branch to delete the file from"
      required: true
  returns: "Commit SHA confirming deletion"

- name: fork_repository
  description: "Fork a GitHub repository to your account or specified organization"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: organization
      type: str
      description: "Organization to fork to"
      required: false
  returns: "Forked repository URL"

- name: get_commit
  description: "Get details for a commit from a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: sha
      type: str
      description: "Commit SHA, branch name, or tag name"
      required: true
    - name: include_diff
      type: bool
      description: "Whether to include file diffs and stats"
      required: false
  returns: "Commit details including author, message, and changed files"

- name: get_file_contents
  description: "Get the contents of a file or directory from a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: path
      type: str
      description: "Path to file/directory"
      required: false
    - name: ref
      type: str
      description: "Git ref (branch, tag, or commit SHA)"
      required: false
  returns: "File content or directory listing"

- name: get_label
  description: "Get a specific label from a repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: name
      type: str
      description: "Label name"
      required: true
  returns: "Label details including name, color, and description"

- name: get_latest_release
  description: "Get the latest release in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
  returns: "Latest release tag, name, and assets"

- name: get_me
  description: "Get details of the authenticated GitHub user"
  parameters: []
  returns: "Authenticated user profile information"

- name: get_release_by_tag
  description: "Get a specific release by its tag name in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: tag
      type: str
      description: "Tag name (e.g., 'v1.0.0')"
      required: true
  returns: "Release details including assets and release notes"

- name: get_tag
  description: "Get details about a specific git tag in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: tag
      type: str
      description: "Tag name"
      required: true
  returns: "Tag details including commit SHA and message"

- name: get_team_members
  description: "Get member usernames of a specific team in an organization"
  parameters:
    - name: org
      type: str
      description: "Organization login that contains the team"
      required: true
    - name: team_slug
      type: str
      description: "Team slug"
      required: true
  returns: "List of team member usernames"

- name: get_teams
  description: "Get details of the teams the user is a member of"
  parameters:
    - name: user
      type: str
      description: "Username to get teams for (defaults to authenticated user)"
      required: false
  returns: "List of teams with organization and role information"

- name: issue_read
  description: "Get information about a specific issue in a GitHub repository"
  parameters:
    - name: method
      type: str
      description: "Read operation: get, get_comments, get_sub_issues, or get_labels"
      required: true
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: issue_number
      type: int
      description: "Issue number"
      required: true
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "Issue details, comments, sub-issues, or labels based on method"

- name: issue_write
  description: "Create a new or update an existing issue in a GitHub repository"
  parameters:
    - name: method
      type: str
      description: "Write operation: create or update"
      required: true
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: title
      type: str
      description: "Issue title"
      required: false
    - name: body
      type: str
      description: "Issue body content"
      required: false
    - name: issue_number
      type: int
      description: "Issue number to update"
      required: false
    - name: state
      type: str
      description: "New state: open or closed"
      required: false
    - name: state_reason
      type: str
      description: "Reason for state change: completed, not_planned, or duplicate"
      required: false
    - name: labels
      type: list
      description: "Labels to apply"
      required: false
    - name: assignees
      type: list
      description: "Usernames to assign"
      required: false
  returns: "Created or updated issue number and URL"

- name: list_branches
  description: "List branches in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of branch names and their commit SHAs"

- name: list_commits
  description: "Get list of commits of a branch in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: sha
      type: str
      description: "Commit SHA, branch or tag name to list commits of"
      required: false
    - name: author
      type: str
      description: "Author username or email to filter by"
      required: false
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of commits with SHA, author, and message"

- name: list_issue_types
  description: "List supported issue types for repository owner (organization)"
  parameters:
    - name: owner
      type: str
      description: "Organization owner of the repository"
      required: true
  returns: "List of available issue types"

- name: list_issues
  description: "List issues in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: state
      type: str
      description: "Filter by state: OPEN or CLOSED"
      required: false
    - name: labels
      type: list
      description: "Filter by labels"
      required: false
    - name: since
      type: str
      description: "Filter by date (ISO 8601 timestamp)"
      required: false
    - name: orderBy
      type: str
      description: "Order by: CREATED_AT, UPDATED_AT, or COMMENTS"
      required: false
    - name: direction
      type: str
      description: "Order direction: ASC or DESC"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of issues with number, title, state, and labels"

- name: list_pull_requests
  description: "List pull requests in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: state
      type: str
      description: "Filter by state: open, closed, or all"
      required: false
    - name: head
      type: str
      description: "Filter by head user/org and branch"
      required: false
    - name: base
      type: str
      description: "Filter by base branch"
      required: false
    - name: sort
      type: str
      description: "Sort by: created, updated, popularity, or long-running"
      required: false
    - name: direction
      type: str
      description: "Sort direction: asc or desc"
      required: false
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of pull requests with number, title, state, and branch info"

- name: list_releases
  description: "List releases in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of releases with tag, name, and publish date"

- name: list_tags
  description: "List git tags in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of tags with name and commit SHA"

- name: merge_pull_request
  description: "Merge a pull request in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: pullNumber
      type: int
      description: "Pull request number"
      required: true
    - name: commit_title
      type: str
      description: "Title for merge commit"
      required: false
    - name: commit_message
      type: str
      description: "Extra detail for merge commit"
      required: false
    - name: merge_method
      type: str
      description: "Merge method: merge, squash, or rebase"
      required: false
  returns: "Merge commit SHA and confirmation"

- name: pull_request_read
  description: "Get information on a specific pull request in GitHub repository"
  parameters:
    - name: method
      type: str
      description: "Read operation: get, get_diff, get_status, get_files, get_review_comments, get_reviews, or get_comments"
      required: true
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: pullNumber
      type: int
      description: "Pull request number"
      required: true
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "Pull request details, diff, status, files, or comments based on method"

- name: pull_request_review_write
  description: "Create, submit, or delete a review on a pull request"
  parameters:
    - name: method
      type: str
      description: "Write operation: create, submit_pending, or delete_pending"
      required: true
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: pullNumber
      type: int
      description: "Pull request number"
      required: true
    - name: body
      type: str
      description: "Review comment text"
      required: false
    - name: event
      type: str
      description: "Review action: APPROVE, REQUEST_CHANGES, or COMMENT"
      required: false
    - name: commitID
      type: str
      description: "SHA of commit to review"
      required: false
  returns: "Review ID and submission status"

- name: push_files
  description: "Push multiple files to a GitHub repository in a single commit"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: branch
      type: str
      description: "Branch to push to"
      required: true
    - name: files
      type: list
      description: "Array of file objects with path and content"
      required: true
    - name: message
      type: str
      description: "Commit message"
      required: true
  returns: "Commit SHA and list of created/updated files"

- name: request_copilot_review
  description: "Request a GitHub Copilot code review for a pull request"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: pullNumber
      type: int
      description: "Pull request number"
      required: true
  returns: "Confirmation that Copilot review was requested"

- name: search_code
  description: "Search for code across GitHub repositories using GitHub code search syntax"
  parameters:
    - name: query
      type: str
      description: "Search query (e.g., 'content:function language:Python org:github')"
      required: true
    - name: sort
      type: str
      description: "Sort field (indexed)"
      required: false
    - name: order
      type: str
      description: "Sort order: asc or desc"
      required: false
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of matching code files with repository and path"

- name: search_issues
  description: "Search for issues in GitHub repositories using issues search syntax"
  parameters:
    - name: query
      type: str
      description: "Search query using GitHub issues search syntax"
      required: true
    - name: owner
      type: str
      description: "Optional repository owner to filter by"
      required: false
    - name: repo
      type: str
      description: "Optional repository name to filter by"
      required: false
    - name: sort
      type: str
      description: "Sort by: comments, reactions, created, updated, etc."
      required: false
    - name: order
      type: str
      description: "Sort order: asc or desc"
      required: false
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of matching issues with repository, number, and title"

- name: search_pull_requests
  description: "Search for pull requests in GitHub repositories"
  parameters:
    - name: query
      type: str
      description: "Search query using GitHub pull request search syntax"
      required: true
    - name: owner
      type: str
      description: "Optional repository owner to filter by"
      required: false
    - name: repo
      type: str
      description: "Optional repository name to filter by"
      required: false
    - name: sort
      type: str
      description: "Sort by: comments, reactions, created, updated, etc."
      required: false
    - name: order
      type: str
      description: "Sort order: asc or desc"
      required: false
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of matching pull requests with repository, number, and title"

- name: search_repositories
  description: "Find GitHub repositories by name, description, topics, or other metadata"
  parameters:
    - name: query
      type: str
      description: "Search query (e.g., 'machine learning language:python stars:>1000')"
      required: true
    - name: sort
      type: str
      description: "Sort by: stars, forks, help-wanted-issues, or updated"
      required: false
    - name: order
      type: str
      description: "Sort order: asc or desc"
      required: false
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of matching repositories with name, description, and stats"

- name: search_users
  description: "Find GitHub users by username, name, or profile information"
  parameters:
    - name: query
      type: str
      description: "Search query (e.g., 'location:seattle followers:>100')"
      required: true
    - name: sort
      type: str
      description: "Sort by: followers, repositories, or joined"
      required: false
    - name: order
      type: str
      description: "Sort order: asc or desc"
      required: false
    - name: page
      type: int
      description: "Page number for pagination"
      required: false
    - name: perPage
      type: int
      description: "Results per page (max 100)"
      required: false
  returns: "List of matching users with username and profile info"

- name: sub_issue_write
  description: "Add, remove, or reprioritize a sub-issue within a parent issue"
  parameters:
    - name: method
      type: str
      description: "Action: add, remove, or reprioritize"
      required: true
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: issue_number
      type: int
      description: "Parent issue number"
      required: true
    - name: sub_issue_id
      type: int
      description: "ID of the sub-issue (not the issue number)"
      required: true
    - name: replace_parent
      type: bool
      description: "Replace sub-issue's current parent (use with add)"
      required: false
    - name: after_id
      type: int
      description: "ID of sub-issue to prioritize after"
      required: false
    - name: before_id
      type: int
      description: "ID of sub-issue to prioritize before"
      required: false
  returns: "Confirmation of sub-issue operation"

- name: update_pull_request
  description: "Update an existing pull request in a GitHub repository"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: pullNumber
      type: int
      description: "Pull request number to update"
      required: true
    - name: title
      type: str
      description: "New title"
      required: false
    - name: body
      type: str
      description: "New description"
      required: false
    - name: state
      type: str
      description: "New state: open or closed"
      required: false
    - name: base
      type: str
      description: "New base branch name"
      required: false
    - name: draft
      type: bool
      description: "Mark as draft (true) or ready for review (false)"
      required: false
    - name: maintainer_can_modify
      type: bool
      description: "Allow maintainer edits"
      required: false
    - name: reviewers
      type: list
      description: "GitHub usernames to request reviews from"
      required: false
  returns: "Updated pull request details"

- name: update_pull_request_branch
  description: "Update the branch of a pull request with the latest changes from the base branch"
  parameters:
    - name: owner
      type: str
      description: "Repository owner"
      required: true
    - name: repo
      type: str
      description: "Repository name"
      required: true
    - name: pullNumber
      type: int
      description: "Pull request number"
      required: true
    - name: expectedHeadSha
      type: str
      description: "Expected SHA of the pull request's HEAD ref"
      required: false
  returns: "Updated branch status and new HEAD SHA"
